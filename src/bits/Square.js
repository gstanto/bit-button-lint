import React from 'react';

const Square = (props) => {
    return (
        <div style={{
            width: 100,
            height: 100,
            color: 'white',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center'
        }}>Square</div>
    )
};

export default Square;
